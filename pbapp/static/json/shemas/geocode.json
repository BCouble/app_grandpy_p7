{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "JSON SCHEMA search of GrandPy in api google maps",
  "anyOf": [{"required": ["formatted_address", "id", "name", "place_id"]}],
  "properties": {
    "results": {
      "properties": {
        "formatted_address": {"type": "string"},
        "id": {"type": "string"},
        "name": {"type": "string"},
        "place_id": {"type": "string"}
      }
    }
  }
}